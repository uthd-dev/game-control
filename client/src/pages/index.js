import styled from 'styled-components';
import Layout from '../components/layout';

function Home () {
    return(
        <Layout>
            <h1>Welcome to UTHD MC!</h1>
        </Layout>
    );
}

export default Home;