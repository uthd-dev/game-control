import Layout from '../layout-components/layout';

function AdminLayout ({children}) {
    return(
        <Layout>
            {children}
        </Layout>
    );
}

export default AdminLayout;